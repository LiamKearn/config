[credential]
    helper = store
[user]
    name = lkearn
    email = liam@sproutlabs.com.au
[alias]
    update = add -u
    sync = !fish -c 'config checkout -p origin/$OTHER --'
[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[add.interactive]
    useBuiltin = false
[delta]
    navigate = true
    light = false
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default

